<%= form_for(@sharelink) do |f| %>
  <% if @sharelink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sharelink.errors.count, "error") %> prohibited this sharelink from being saved:</h2>

      <ul>
      <% @sharelink.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_field :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
